function.spt_list_unique.description=Returns the unique elements found in the provided list. If the list is null or empty, returns null. If not a List, returns what was passed in. By default, null elements are removed but can be kept by setting keepNulls to true.
function.spt_list_unique.param.list.description=The list to unique
function.spt_list_unique.param.keepNulls.description=The list to unique