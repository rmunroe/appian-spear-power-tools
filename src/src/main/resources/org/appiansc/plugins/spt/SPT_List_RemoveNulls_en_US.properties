function.spt_list_removenulls.description=Removes all null elements from the given list. If a List of Text (string) is passed it, removes empty strings ("") as well. If not a List, returns what was passed in.
function.spt_list_removenulls.param.list.description=The list to remove nulls from